import { Route } from '@angular/router';
import { LiveTrackingComponent } from './live-tracking.component';

export const liveTrackingRoutes: Route[] = [
    {
        path     : '',
        component: LiveTrackingComponent
    }
];
