# ------------------------------
# FRONTEND: pcs-zambia.com + www
# ------------------------------

# HTTP: ACME + redirect to HTTPS
server {
  listen 80;
  listen [::]:80;
  server_name pcs-zambia.com www.pcs-zambia.com;

  # ACME challenges (matches docker volume: ./nginx/certs/.well-known -> /acme-webroot/.well-known)
  location ^~ /.well-known/acme-challenge/ {
    root /acme-webroot;
    default_type "text/plain";
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

# HTTPS: proxy to Angular container "frontend"
server {
  listen 443 ssl http2;
  listen [::]:443 ssl http2;
  server_name pcs-zambia.com www.pcs-zambia.com;

  ssl_certificate     /etc/letsencrypt/live/pcs-zambia.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/pcs-zambia.com/privkey.pem;

  # (optional hardening)
  ssl_session_timeout 1d;
  ssl_session_cache shared:SSL:10m;
  ssl_protocols TLSv1.2 TLSv1.3;

  location / {
    proxy_pass http://frontend;      # << Angular container (port 80 inside that container)
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;

    # SPA fallback (uncomment if needed)
    # error_page 404 =200 /index.html;
  }
}

# ------------------------------
# API: api.pcs-zambia.com
# ------------------------------

# HTTP: ACME + redirect to HTTPS
server {
  listen 80;
  listen [::]:80;
  server_name api.pcs-zambia.com;

  location ^~ /.well-known/acme-challenge/ {
    root /acme-webroot;
    default_type "text/plain";
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

# HTTPS: proxy to NestJS container "app:3000"
server {
  listen 443 ssl http2;
  listen [::]:443 ssl http2;
  server_name api.pcs-zambia.com;

  ssl_certificate     /etc/letsencrypt/live/pcs-zambia.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/pcs-zambia.com/privkey.pem;

  ssl_session_timeout 1d;
  ssl_session_cache shared:SSL:10m;
  ssl_protocols TLSv1.2 TLSv1.3;

  location / {
    proxy_pass http://app:3000;
    proxy_http_version 1.1;

    # WebSocket support
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    # Forwarded headers
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;

    proxy_read_timeout 300;
  }
}