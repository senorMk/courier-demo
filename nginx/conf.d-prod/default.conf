# ------------------------------
# FRONTEND: platinumcourier.co.zm + www + wildcard
# ------------------------------

# HTTP: ACME + redirect to HTTPS
server {
  listen 80;
  listen [::]:80;
  server_name platinumcourier.co.zm www.platinumcourier.co.zm *.platinumcourier.co.zm;

  # ACME challenges (matches docker volume: ./nginx/certs/.well-known -> /acme-webroot/.well-known)
  location ^~ /.well-known/acme-challenge/ {
    root /acme-webroot;
    default_type "text/plain";
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

# HTTPS: proxy to Angular container "frontend"
server {
  listen 443 ssl http2;
  listen [::]:443 ssl http2;
  server_name platinumcourier.co.zm www.platinumcourier.co.zm *.platinumcourier.co.zm;

  ssl_certificate     /etc/ssl/platinumcourier/platinumcourier_fullchain.crt;
  ssl_certificate_key /etc/ssl/platinumcourier/platinumcourier_wildcard.key;

  # Best-practice SSL settings
  ssl_session_timeout 1d;
  ssl_session_cache shared:SSL:10m;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers on;

  location / {
    proxy_pass http://frontend;      # << Angular container (port 80 inside that container)
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;

    # SPA fallback (uncomment if needed)
    # error_page 404 =200 /index.html;
  }
}

# ------------------------------
# API: api.platinumcourier.co.zm
# ------------------------------

# HTTP: ACME + redirect to HTTPS
server {
  listen 80;
  listen [::]:80;
  server_name api.platinumcourier.co.zm;

  location ^~ /.well-known/acme-challenge/ {
    root /acme-webroot;
    default_type "text/plain";
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

# HTTPS: proxy to NestJS container "app:3000"
server {
  listen 443 ssl http2;
  listen [::]:443 ssl http2;
  server_name api.platinumcourier.co.zm;

  ssl_certificate     /etc/ssl/platinumcourier/platinumcourier_fullchain.crt;
  ssl_certificate_key /etc/ssl/platinumcourier/platinumcourier_wildcard.key;

  ssl_session_timeout 1d;
  ssl_session_cache shared:SSL:10m;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers on;

  location / {
    proxy_pass http://app:3000;
    proxy_http_version 1.1;

    # WebSocket support
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    # Forwarded headers
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;

    proxy_read_timeout 300;
  }
}
